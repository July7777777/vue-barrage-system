interface AppConfig {
  appKey: string;
  appSecret: string;
  codeId: string;
  appId: string;
}

const config: AppConfig = {
  appKey: '',
  appSecret: '',
  // 替换你的主播身份码
  codeId: '',
  // 替换你的app应用 
  appId: '',
};

export default config;
